{
  "name": "section1-training-app",
  "version": "1.0.0",
  "description": "this is just a backend that will handle simple client-server requests. No Auth. Just some routes that handles how the client requests and manages the workouts",
  "license": "ISC",
  "author": "me",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "server": "node index.js",
    "nodemon": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "mongoose": "^9.2.1"
  }
}
